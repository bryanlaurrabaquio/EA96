#include <stdio.h>
int main() {
  int validatePositiveInt;
  int a;
  printf("Ingresa un numero entero: ");
  scanf("%c", &a);
  validatePositiveInt = a;

  void generateEvenNumbers(int limit); {
    printf("Los numeros pares son: %i\n");
    int limit;
    for (int i = 1; i <= limit; i++) {
      if (i % 2 == 0) {
      }
    }
  }
  printf("%i\n", a);

  printf("Desea repetir el programa para salir?\n");
  scanf("%i",&a);
  return 0;
}

